{
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build":"npm run build", 
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "libsodium-wrappers": "^0.7.15",
    "upper-case": "^3.0.0"
  }
}
